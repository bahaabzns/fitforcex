import DeliveryPolicyContent from './DeliveryPolicyContent';

export const metadata = {
    title: 'Delivery & Shipment Policy · FitForce',
    description: 'How digital access to FitForce is delivered — no physical products are shipped.',
};

export default function DeliveryPolicyPage() {
    return <DeliveryPolicyContent />;
}
